# Dev / tooling scripts — không cần deploy lên Vercel
# (Các file này trước đây đã được commit nên .gitignore không có tác dụng;
#  đã chạy `git rm --cached` để bỏ theo dõi, file vẫn còn trên máy.)
deep-translate.py
fix-translations.py
design_all_files.json
design_visual_titles.json
vua2hand_visual_titles.json
scratch/

# File nguồn thiết kế (PSD/PSB/ảnh gốc chưa nén) — KHÔNG commit.
# Repo hiện đang mang ~108 MB file nguồn không dùng tới; xem README mục
# "Dọn dẹp repo" để biết cách tách chúng ra khỏi lịch sử git.
*.psd
*.psb
*.ai
*.prproj
*.aep

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
.claude/
